@model List<Search4Support.Models.ServiceTag>

@if (Model.Count == 0)
{
    <h1>No elements with the given tag.</h1>
}
else
{
    <h1>Services Tagged: @Model[0].Tag.Name</h1>

    <ul>
        @foreach (var srvTag in Model)
        {
            <li>@srvTag.Service.Name</li>
        }
    </ul>

}