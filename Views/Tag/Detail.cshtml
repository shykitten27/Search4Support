@model List<Search4Support.Models.ServiceTag>

@if (Model.Count == 0)
{
    <h2>No elements with the given tag.</h2>
}
else
{
    <h2>Services Tagged: @Model[0].Tag.Name</h2>

    <ul>
        @foreach (var srvTag in Model)
        {
            <li>@srvTag.Service.Name</li>
        }
    </ul>

}